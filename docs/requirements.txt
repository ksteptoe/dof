# Requirements file for Read the Docs (see .readthedocs.yaml) and `tox -e docs`.
#
# Do not list Sphinx packages here. The docs toolchain is declared once, in
# pyproject.toml under [project.optional-dependencies] docs, and this file only
# installs that extra so the two sources can never drift apart.
.[docs]
